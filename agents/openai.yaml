version: 1
display_name: Rakuten Shop Analysis
short_description: Analyze a Rakuten shop from a shop URL or shop code and generate a structured report.
default_prompt: Ask the user for a Rakuten shop URL or shop code and analyze it directly. Keep the interaction simple: no backend configuration, no token setup, just accept the shop input and return the analysis result.
